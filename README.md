# PoC A - Node Application
Node application repository for PoC A validation.

## Configuration

Project-specific configuration is defined in `config/poc-node-app.yml`:
- Node version: 18
- Tests enabled
- SonarQube enabled

## CI/CD

Powered by centralized reusable workflows from `poc-central-pipelines`.

See `.github/workflows/ci.yml` for the workflow definition.
